import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        width: '100%',
    },
    container: {
        maxHeight: 440,
        alignContent: 'right'
    },
    button: {
        height: 40
    }
});
export default useStyles